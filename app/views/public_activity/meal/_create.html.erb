added a new meal: 
<% if activity.trackable %>
	<%= link_to image_tag(activity.trackable.picture, size: "60", class: "pull-right"), activity.trackable %>
<% else %>
	which has since been removed
<% end %>